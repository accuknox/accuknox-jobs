apiVersion: v1
kind: ConfigMap
metadata:
  name: kubearmor-vars
  namespace: {{ .Release.Namespace }}
data:
  {{- range $k, $v := .Values.feederService.kubearmorVars }}
  {{- if $v }}
  {{ $k }}: {{ $v | quote -}}
  {{- else }}
  {{ $k }}:  ""
  {{- end }}
  {{- end }} 
