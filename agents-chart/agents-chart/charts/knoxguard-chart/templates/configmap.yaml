apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "chart.fullname" . }}-controller-config
  labels:
    control-plane: controller-manager
  {{- include "chart.labels" . | nindent 4 }}
data:
  config.yaml: |-
{{- include "policyController" . }}