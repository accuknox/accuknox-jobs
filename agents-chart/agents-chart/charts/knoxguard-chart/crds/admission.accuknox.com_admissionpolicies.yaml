---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: admissionpolicies.admission.accuknox.com
spec:
  group: admission.accuknox.com
  names:
    kind: AdmissionPolicy
    listKind: AdmissionPolicyList
    plural: admissionpolicies
    shortNames:
    - admpol
    singular: admissionpolicy
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type == "Ready")].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type == "Ready")].message
      name: MESSAGE
      type: string
    - jsonPath: .status.policies
      name: OWNED_PPLICIES
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: AdmissionPolicy is the Schema for the policies API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AdmissionPolicySpec defines the desired state of Policy
            properties:
              apparmor:
                properties:
                  action:
                    default: Audit
                    description: |-
                      action defines the enforcement mode in case if policy got violated
                      accpeted values are Audit and Block default set to Audit
                    enum:
                    - Audit
                    - Block
                    type: string
                  ignoreNamespaces:
                    description: 'optional: ignore namespace(s) this rule should not
                      applied to'
                    items:
                      type: string
                    type: array
                  targetNamespaces:
                    description: 'optional: target namespace(s) this rule applied
                      to'
                    items:
                      type: string
                    type: array
                type: object
              hostNamespaces:
                properties:
                  action:
                    default: Audit
                    description: |-
                      action defines the enforcement mode in case if policy got violated
                      accpeted values are Audit and Block default set to Audit
                    enum:
                    - Audit
                    - Block
                    type: string
                  ignoreNamespaces:
                    description: 'optional: ignore namespace(s) this rule should not
                      applied to'
                    items:
                      type: string
                    type: array
                  targetNamespaces:
                    description: 'optional: target namespace(s) this rule applied
                      to'
                    items:
                      type: string
                    type: array
                type: object
              hostPath:
                properties:
                  action:
                    default: Audit
                    description: |-
                      action defines the enforcement mode in case if policy got violated
                      accpeted values are Audit and Block default set to Audit
                    enum:
                    - Audit
                    - Block
                    type: string
                  ignoreNamespaces:
                    description: 'optional: ignore namespace(s) this rule should not
                      applied to'
                    items:
                      type: string
                    type: array
                  targetNamespaces:
                    description: 'optional: target namespace(s) this rule applied
                      to'
                    items:
                      type: string
                    type: array
                type: object
              hostPort:
                properties:
                  action:
                    default: Audit
                    description: |-
                      action defines the enforcement mode in case if policy got violated
                      accpeted values are Audit and Block default set to Audit
                    enum:
                    - Audit
                    - Block
                    type: string
                  ignoreNamespaces:
                    description: 'optional: ignore namespace(s) this rule should not
                      applied to'
                    items:
                      type: string
                    type: array
                  targetNamespaces:
                    description: 'optional: target namespace(s) this rule applied
                      to'
                    items:
                      type: string
                    type: array
                type: object
              hostProcess:
                properties:
                  action:
                    default: Audit
                    description: |-
                      action defines the enforcement mode in case if policy got violated
                      accpeted values are Audit and Block default set to Audit
                    enum:
                    - Audit
                    - Block
                    type: string
                  ignoreNamespaces:
                    description: 'optional: ignore namespace(s) this rule should not
                      applied to'
                    items:
                      type: string
                    type: array
                  targetNamespaces:
                    description: 'optional: target namespace(s) this rule applied
                      to'
                    items:
                      type: string
                    type: array
                type: object
              privilegeEscalation:
                properties:
                  action:
                    default: Audit
                    description: |-
                      action defines the enforcement mode in case if policy got violated
                      accpeted values are Audit and Block default set to Audit
                    enum:
                    - Audit
                    - Block
                    type: string
                  ignoreNamespaces:
                    description: 'optional: ignore namespace(s) this rule should not
                      applied to'
                    items:
                      type: string
                    type: array
                  targetNamespaces:
                    description: 'optional: target namespace(s) this rule applied
                      to'
                    items:
                      type: string
                    type: array
                type: object
              privilegedPod:
                properties:
                  action:
                    default: Audit
                    description: |-
                      action defines the enforcement mode in case if policy got violated
                      accpeted values are Audit and Block default set to Audit
                    enum:
                    - Audit
                    - Block
                    type: string
                  ignoreNamespaces:
                    description: 'optional: ignore namespace(s) this rule should not
                      applied to'
                    items:
                      type: string
                    type: array
                  targetNamespaces:
                    description: 'optional: target namespace(s) this rule applied
                      to'
                    items:
                      type: string
                    type: array
                type: object
              procMount:
                properties:
                  action:
                    default: Audit
                    description: |-
                      action defines the enforcement mode in case if policy got violated
                      accpeted values are Audit and Block default set to Audit
                    enum:
                    - Audit
                    - Block
                    type: string
                  ignoreNamespaces:
                    description: 'optional: ignore namespace(s) this rule should not
                      applied to'
                    items:
                      type: string
                    type: array
                  targetNamespaces:
                    description: 'optional: target namespace(s) this rule applied
                      to'
                    items:
                      type: string
                    type: array
                type: object
              registries:
                properties:
                  action:
                    default: Audit
                    description: |-
                      action defines the enforcement mode in case if policy got violated
                      accpeted values are Audit and Block default set to Audit
                      when a registry rule is defined using include and exclude registry
                      action (Audit/Enforce) defines the policy violation behaviour
                    enum:
                    - Block
                    - Audit
                    type: string
                  exclude:
                    description: exclude list contains the list of untrusted/blacklisted
                      registries
                    items:
                      properties:
                        namespace:
                          default: all
                          type: string
                        pattern:
                          type: string
                      type: object
                    type: array
                  include:
                    description: include list contains the list of trusted/whitelisted
                      registries/images
                    items:
                      properties:
                        namespace:
                          default: all
                          type: string
                        pattern:
                          type: string
                      type: object
                    type: array
                type: object
              runAsNonRoot:
                properties:
                  action:
                    default: Audit
                    description: |-
                      action defines the enforcement mode in case if policy got violated
                      accpeted values are Audit and Block default set to Audit
                    enum:
                    - Audit
                    - Block
                    type: string
                  ignoreNamespaces:
                    description: 'optional: ignore namespace(s) this rule should not
                      applied to'
                    items:
                      type: string
                    type: array
                  targetNamespaces:
                    description: 'optional: target namespace(s) this rule applied
                      to'
                    items:
                      type: string
                    type: array
                type: object
              seccomp:
                properties:
                  action:
                    default: Audit
                    description: |-
                      action defines the enforcement mode in case if policy got violated
                      accpeted values are Audit and Block default set to Audit
                    enum:
                    - Audit
                    - Block
                    type: string
                  ignoreNamespaces:
                    description: 'optional: ignore namespace(s) this rule should not
                      applied to'
                    items:
                      type: string
                    type: array
                  targetNamespaces:
                    description: 'optional: target namespace(s) this rule applied
                      to'
                    items:
                      type: string
                    type: array
                type: object
              selinux:
                properties:
                  action:
                    default: Audit
                    description: |-
                      action defines the enforcement mode in case if policy got violated
                      accpeted values are Audit and Block default set to Audit
                    enum:
                    - Audit
                    - Block
                    type: string
                  ignoreNamespaces:
                    description: 'optional: ignore namespace(s) this rule should not
                      applied to'
                    items:
                      type: string
                    type: array
                  targetNamespaces:
                    description: 'optional: target namespace(s) this rule applied
                      to'
                    items:
                      type: string
                    type: array
                type: object
              sysctls:
                properties:
                  action:
                    default: Audit
                    description: |-
                      action defines the enforcement mode in case if policy got violated
                      accpeted values are Audit and Block default set to Audit
                    enum:
                    - Audit
                    - Block
                    type: string
                  ignoreNamespaces:
                    description: 'optional: ignore namespace(s) this rule should not
                      applied to'
                    items:
                      type: string
                    type: array
                  targetNamespaces:
                    description: 'optional: target namespace(s) this rule applied
                      to'
                    items:
                      type: string
                    type: array
                type: object
              vulnerability:
                properties:
                  action:
                    default: Audit
                    description: |-
                      action defines the enforcement mode in case if policy got violated
                      accpeted values are Audit and Block default set to Audit
                    enum:
                    - Block
                    - Audit
                    type: string
                  exceptions:
                    description: |-
                      execption rule can be use to define exemptive threshold rules for
                      particular images
                    items:
                      properties:
                        pattern:
                          description: image pattern
                          type: string
                        thresholds:
                          properties:
                            critical:
                              type: integer
                            high:
                              type: integer
                            medium:
                              type: integer
                          type: object
                      type: object
                    type: array
                  ignoreIfDataNotPresent:
                    default: true
                    description: |-
                      if vulnerability data is not present for processed image
                      weather it should be ignored
                    type: boolean
                  thresholds:
                    description: |-
                      thresholds defines the accepted thresholds for critical, high and medium
                      severtiy vulnarabilities
                    properties:
                      critical:
                        type: integer
                      high:
                        type: integer
                      medium:
                        type: integer
                    type: object
                type: object
            type: object
          status:
            description: AdmissionPolicyStatus defines the observed state of Policy
            properties:
              conditions:
                description: |-
                  INSERT ADDITIONAL STATUS FIELD - define observed state of cluster
                  Important: Run "make" to regenerate code after modifying this file
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              policies:
                items:
                  type: string
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
