kmux:
  sink:
    stream: knox-gateway
  security: spire
    
knox-gateway:
  server: {{ include "agents.knoxGatewayHost" . }}

spire: 
  spiffeID:
    format: "spiffe://<domainName>/<WSID>/<ClusterID>/<componentName>"