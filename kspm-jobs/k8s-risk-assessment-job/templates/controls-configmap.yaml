{{- if .Values.controlsConfig.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: controls-config-{{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
data:
  controls-config.json: |
{{ .Files.Get "templates/default_config_inputs.json" | indent 4 }}
{{- end }}